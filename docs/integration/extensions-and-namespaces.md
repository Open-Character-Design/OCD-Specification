# Extensions & Namespaces

- Use `x-*` for system-specific fields (e.g., `x-dnd5e`)
- Document structure and ranges in the extension’s own README
- Avoid duplicating core semantics; propose core changes via PR when broadly useful
