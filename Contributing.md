# Contributing

1. Fork, create a feature branch, write tests.
2. Keep docs/spec changes atomic and referenced in commit messages.
3. Run tests: `cd python && hatch run test`.
4. Open a PR; describe rationale and any schema/grammar impacts.
